## Боты

- [Борис](https://vk.com/royal_boris) добавляет людей в беседы. Для этого нужно, чтобы они вначале добавили его в друзья. Также выполняет команду замены фото беседы (но принимает эту команду Секретарь)
- [Секретарь](https://vk.com/public174286655) выполняет все команды. Именно ему нужно писать команды в личку

В некоторых беседах Борис сам отвечает на команды. Эта фукнция убрана для новых бесед, но для удобства оставлена в старых.

## Требуемые права

- 🧑 Участник беседы (нет особых прав)
- 🔨 Модератор (может исключать участников и управлять ими)
- 🔑 Супер-модератор (все команды модераторов, плюс может управлять модераторами)
- 👑 Администратор (все команды; не путать с администраторами бесед ВК, которые были введены летом 2018 года)
- ⛔ Оператор бота (владелец)

## Используемые обозначения

- 🤖 Команду можно писать в личку боту
- `/команда <аргумент>` Здесь внутри `<>` приводится название/описание аргумента. На месте `<аргумент>` надо написать то, что требует данная команда, без всяких угловых скобок `<>`
- `<пользователь>` — ссылка на профиль пользователя, с которым надо выполнить какое-то действие, например, [vk.com/id1](vk.com/id1). Также можно использовать упоминание, или вместе с командой переслать его сообщение
- `<пользователи>` — команду можно применить сразу к нескольким пользователям
- `<длительность>` — время, записанное как целое число, за которым следует одна из букв `s` (секунды), `m` (минуты), `h` (часы) или `d` (дни)
- `<игра>` — или `cr` (то есть Clash Royale), или `bs` (то есть Brawl Stars)
- `#тег` — тег игрока или клана в Clash Royale (CR)
- `<on/off>` — или слово `on` (да), или `off` (нет)

## Общие команды

- 🧑🤖 `/help` Получить ссылку на эту страницу
- 🧑🤖 `/ping`, `/pong` Проверить, работает ли бот
- 🧑🤖 `/command_link <команда>` Создать ссылку на личку бота. Если перейти по ней и написать любое сообщение, то автоматически выполнится команда, следующая далее. Например: `/make_command_link /ping`
- 🧑🤖 `/start` Борис посылает приглашение в друзья. Также выводит инструкции по началу пользования ботом
- 🧑🤖 `/friend_me` Борис посылает приглашение в друзья
- ⛔🤖 `/shutdown` Выключает бота
- ⛔🤖 `/maintenance` Выполнить текущую операцию обслуживания бота

## Приглашение и исключение из беседы

- 🔨 `/invite <пользователи>` Пригласить в беседу. Борис должен быть у приглашаемого в друзьях. Если пользователь направил приглашение Борису, то он его автоматически примет при выполнении этой команды. Команда умеет "пробивать" самостоятельный выход из беседы
- 🔨 `/kick <пользователи>` Кикнуть из беседы. Кик бессрочный. Позже кика пользователя можно будет пригласить обратно
- 🔨 `/ban <длительность> <пользователи>` Забанить на определённый срок. Пользователь исключается из беседы, и ему присваивается специальная роль `banned`. По прошествии указанного срока, пользователь возвращается в беседу. Пользователя с ролью `banned` нельзя пригласить в беседу через `/invite` или вручную. При повторном применении `/ban` к уже забаненному пользователю, он перебанивается, начиная с текущего момента
- 🔨 `/ban perm <пользователи>` Забанить навсегда. Такой пользователь не будет автоматически возвращён в беседу
- 🔨 `/unban <пользователи>` Разбанить. При этом с пользователей снимается роль `banned` и они приглашаются в беседу
- 🧑 `/banned` Посмотреть список забаненных пользователей, вместе с временем, оставшимся до автоматического разбана

## Роли

По аналогии с Discord, любому участнику беседы можно присвоить сколько угодно ролей. В каждой беседе у каждого пользователя "местный", независимый от других бесед набор ролей.

Системные роли:

- `moderator` Модератор
- `supermoderator` Супер-модератор
- `admin` Администратор
- `banned` Забаненный

Управление ролями:

- 🧑 `/roles` Показать список ролей беседы
- 👑 `/add_role <название роли>` Создать новую роль
- 👑 `/remove_role <название роли>` Удалить роль
- 👑 `/name_role <старое название роли> <новое название роли>` Поменять название роли
- 👑 `/move_role <роль> <номер>` Передвинуть роль в списке ролей на позицию с указанным номером

Выдача ролей:

- 🧑 `/role <роль>` Показать список участников с указанной ролью
- 🧑 `/mention <роль>` Призвать в беседу участников с указанной ролью
- 🔨 `/give <роль> <пользователи>` Выдать указанную роль пользователям
- 🔑 `/give moderator <пользователи>`
- 👑 `/give supermoderator <пользователи>`
- 👑 `/give admin <пользователи>`
- 🔨 `/take <роль> <пользователи>` Забрать указанную роль у пользователей
- ⛔ `/force_give <роль> <пользователи>` Выдать любую роль пользователям (не требуется `admin`)
- ⛔ `/force_take <роль> <пользователи>` Забрать любую роль у пользователей, включая `admin` (не требуется `admin`)

Чтобы иметь право кикнуть участника беседы или управлять его ролями, привязками к аккаунтам или описанием, вы должны иметь административную роль выше него. Исключение: `admin` может управлять информацией о других `admin`. Выдавайте роль `admin` осторожно: забрать её возможно только при помощи оператора бота!

## Настройка названия и фото беседы

- 👑 `/set_title` Изменить название беседы. Если не-`admin` изменит название беседы не через бота, то бот вернёт его обратно
- 👑 `/free_title` Разрешить изменять название беседы вручную
- 👑 `/set_photo` Изменить фото беседы. Если не-`admin` изменит фото беседы не через бота, то бот вернёт его обратно
- 👑 `/free_title` Разрешить изменять фото беседы вручную

## Настройка автоматического бана

- 👑 `/ban_duration <длительность>` Задать длительность автоматического временного бана
- 👑 `/swear_ban <on/off>` Включить/выключить бан за ругательства. Слова, считающиеся ругательствами, можно посмотреть [здесь](swear_words.txt). В частности, там есть слово `кикменя`, которое можно использовать, чтобы исключиться из беседы на стандартное время
- 👑 `/voice_ban <on/off>` Включить/выключить бан за голосовые сообщения
- 👑 `/graffiti_ban <on/off>` Включить/выключить бан за граффити
- 👑 `/coffi_ban <on/off>` Включить/выключить бан за шифровки от Coffi

## Борьба со спамом командами

- 👑 `/respond_to_pm <on/off>` Включить/выключить ответы на команды в личку. Ответы на не-административные команды будут посылаться в личку тому, кто написал команду. Используется для уменьшения спаса командами вроде `/chests`. Чтобы личное сообщение дошло до участника, он должен вначале написать Секретарю любое сообщение в личку
- 🧑 `/pm <команда>` Модификатор команды, который заставляет Секретаря ответить личным сообщением, независимо от настроек беседы. Пример: `/pm /chests`
- 🧑 `/here <команда>` Модификатор команды, который заставляет Секретаря ответить в беседе, независимо от настроек беседы. Пример: `/here /chests`
- ⛔ `/auto_delete_after <on/off>` Заставить бота автоматически удалять свои ответы на команды по таймеру. Работает только в беседах, где Борис настроен так, что сам отвечает на команды вместо Секретаря
- ⛔ `/remove_auto_delete <on/off>` Выключить автоудаление сообщений по таймеру

## Создание новых бесед

- 🧑 `/sync` Активирует всех ботов, присутствующих в беседе. Нужно выполнить после добавления бота (ботов) в беседу и выдачи нужных прав (обычно права администратора беседы ВК). При этом никто не получает никаких ролей: в беседе нет ни админов, ни модераторов
- ⛔🤖 `/create_chat` Борис Борисович создаёт и активирует беседу. Для полноценного функционирования требуется вручную зайти на аккаунт Бориса, добавить в беседу Секретаря, выдать ему права, после чего активировать беседу с помощью `/sync`
- ⛔ `/use_secretary <on/off>` Включить/выключить использование Секретаря (можно заставить самого Бориса отвечать на команды)

## Привязка акканутов CR и BS

Модераторы бесед могут привязать к любому пользователю ВК его аккаунты Clash Royale.

- 🔨 `/add_tag <пользователь> <игра> #тег-игрока` Привязать пользователю аккаунт CR
- 🔨 `/remove_tag <пользователь> <игра> #тег-игрока` Отвязать от пользователя аккаунт CR
- 🔨 `/clear_tags <пользователь>` Отвязать от пользователя все аккаунты CR

После этого можно будет легко смотреть, кто он такой, через другие команды.

- 🧑🤖 `/tags <пользователь>` Показать теги аккаунтов CR указанного пользователя
- 🧑🤖 `/profile <пользователь>` Показать ссылки на аккаунты CR указанного пользователя
- 🧑🤖 `/trophies <пользователь>` Показать количество трофеев на аккаунтах CR указанного пользователя
- 🧑🤖 `/chests <пользователь>` Показать будущие сундуки на аккаунтах CR указанного пользователя
- 🧑🤖 `/whois <пользователь>` Показать кубки, ники и кланы всех аккаунтов CR указанного пользователя. Если команда выполняется в беседе, то также показывает роли этого участника беседы

Все 5 команд выше можно применять к самому себе, если не указать пользователя.

## Подтверждение аккаунтов CR и BS

Пользователь может самостоятельно привязать и подтвердить себе аккаунт. Рядом с подтверждёнными аккаунтами в `/whois` отображается значок ✅.

- 🤖 `/verify_tag <игра> #тег-игрока` Подтвердить ваше владение аккаунтом. Бот потребует создать с этого аккаунта клан со специальным названием и описанием. В CR эта операция стоит 1000 золота, в BS бесплатно
- 🤖 `/unverify_tag <игра> #тег-игрока` Отменить подтверждение аккаунта

## Другие команды CR и BS

- 🧑🤖 `/find_nick <ник или часть ника>` Поиск пользователя ВК по привязанному к нему нику
- 🧑🤖 `/player_decks cr #тег-игрока` Ссылки на колоды CR, которые игрок использовал в последнее время, вместе с описанием, против каких колод эта дека хороша и плоха

## Настройка беседы

- 🧑 `/chat_settings` Названия и текущие значения настроек

Настройки, которые не вошли в другие категории команд:

- 👑 `/invite_protection <on/off>` Если включено, то добавлять людей в беседу можно только через бота. Добавленных вручную будет кикать
- 👑 `/hide_royale <on/off>` Если включено, команды, связанные с CR, недоступны, и в `/whois` не отображаются профили CR
- 👑 `/default_clan <игра> #тег-клана` Задать клан CR по умолчанию. Командам из раздела "Управление кланами CR" не обязательно будет указывать клан
- 👑 `/remove_default_clan` Убрать клан CR по умолчанию. Клан обязательно будет указывать во всех командах из раздела "Управление кланами"
- 👑 `/clan_warning <on/off>` Показывать предупреждение о полном клане и несоответствующем пороге для клана по умолчанию
- 👑 `/delete_chat` Удалить все настройки, роли, сокращения и т.д., а также деактивировать беседу. Чтобы бот вновь заработал, напишите `/sync`

## Управление кланами Clash Royale

- 🧑🤖 `/clan_status <игра> #тег-клана` Краткая сводка по клану
- 🔨🤖 `/clan_info <игра> #тег-клана` Текущие кубки и донат всех игроков клана
- 🔨🤖 `/clan_history <игра> #тег-клана <число недель>` Кубки и донат всех игроков на момент нескольких недель назад. История клана должна быть предварительно [активирована](https://docs.royaleapi.com/#/endpoints/clan_history?id=how-to-be-included) и работать на протяжении минимум недели
- 🔨🤖 `/full_donation_history <игра> #тег-клана` Донат всех игроков клана на протяжении последних 6 недель. Слева новые недели, справа старые, текущей недели нет
- 🧑🤖 `/clan_donation_history <игра> #тег-клана` Донат топ-10 игроков (по донату) на протяжении последних 6 недель. Слева новые недели, справа старые, текущей недели нет
- 🧑🤖 `/low_members <игра> #тег-клана` Список участников клана (без стариков и соруков) с наименьшими кубками, которые не участвуют в КВ и которых можно сейчас кикать. Наверху списка кубки меньше, снизу больше
- 🧑🤖 `/war_losers <игра> #тег-клана` Список игроков клана с наименьшим процентом выигранных КВ в этом клане. Формат: [кубки] выиграно/всего
- 🧑🤖 `/war_winners <игра> #тег-клана` Список игроков клана с наибольшим процентом выигранных КВ в этом клане. Формат: [кубки] выиграно/всего

Если для беседы задан клан по умолчанию (`/default_clan`), то `<игра> #тег-клана` можно не указывать.

## Сокращения

Сокращения — это ключевые слова, при появлении которых в беседе бот напишет заранее заготовленное сообщение. Например:

- Админ: `/set_shortcut #правила Наши правила: не оскорблять участников беседы.`
- ...
- <Новый пользователь заходит в беседу>
- Админ: `#правила`
- Бот: `Наши правила: не оскорблять участников беседы.`

Сокращение не обязательно должно содержать `#`.

- 👑 `/set_shortcut <название сокращения> <текст>` Добавить сокращение
- 👑 `/remove_shortcut <название сокращения>` Удалить сокращение
- 👑 `/show_shortcut <название сокращения>` Показать содержимое сокращения

2 особых сокращения, которые можно задать — это `welcome` и `goodbye`, которые бот будет писать при добавлении и при выходе пользователя из беседы.

## Описания

Описание пользователя, если оно у него имеется, выводится в `/whois`.

- 👑 `/set_description <пользователь> <текст описания>` Задаёт описание 
- 👑 `/remove_description <пользователь> <текст описания>`

## Списки

- 🧑 `/shortcuts` Список сокращений
- 🧑 `/roles` Список ролей
- 🔨 `/all_roles` Список всех обладателей каждой из ролей
- 🧑 `/all_tags` Список всех аккаунтов всех участников беседы
- 🧑 `/all_clans` Список кланов, в которых состоят все участники беседы
- 🧑 `/channels` Список каналов сервера беседы (если беседы состоит в сервере)
- 🧑 `/all_trophies` Список рекорда по кубкам всех участников беседы

## Рейтинг

Рейтинг участника беседы на текущий момент состоит из количества отправленных сообщений и их среднего размера (в символах)

- 🧑 `/rating` Ваш рейтинг в беседе
- 🧑 `/top_ratings` Список пользователей с самым большим рейтингом

## Серверы

Беседы можно объединять в серверы. В составе серверов, беседам присваиваются короткие имена (также известны как каналы; не путать с заголовком беседы), которые используются в командах.

- 👑 `/create_server <имя-сервера>` Создать новый сервер на базе текущей беседы. Имя сервера может содержать только буквы, цифры, дефисы и подчёркивания (но не пробелы), и иметь длину от 1 до 30 символов. Также имя сервера не может совпадать с именами уже созданных серверов. Новый сервер после создания будет состоять из одной беседы с именем `main` — текущей беседы
- 👑 `/attach_channel <имя-сервера> <имя-канала>` Присоединить беседу к существующему серверу, а также задать короткое имя беседы

После присоединения к серверу можно выполнять следующие команды:

- 🧑 `/channels` Показывает название сервера и всех бесед. Текущая беседа помечена `[here]`. Беседы, куда добавляются участники при `/accept`, помечены `[welcome]`. Беседы, где включён лог команд, помечены `[log]`
- 👑 `/channel_name <новое-имя-канала>` Изменить короткое имя текущей беседы
- 👑 `/server_name <новое-имя-сервера>` Изменить название сервера
- 👑 `/detach_channel` Отсоединить беседу от сервера

Основные функции серверов:

- 🧑 `/go <имя-канала>` Перейти в другой канал сервера
- 🧑 `/to <имя-канала> <команда>` Выполнить команду в другой беседе сервера. Например, `/to main /kick https://vk.com/id1` кикнет пользователя из беседы с коротким именем `main`, но не из текущей беседы
- 👑 `/log_commands <on/off>` Включить пересылку всех исполняемых команд сервера в текущую беседу. Полезно для контроля за модераторами. Маловажные команды, которые ничего не меняют, не пересылаются

## Запросы на вступление в сервер

- 🧑🤖 `/join <имя сервера>` Отправляет запрос на вступление в сервер. Имя сервера узнайте у участников беседы
- 🧑🤖 `/join <имя сервера> <игра> #тег-игрока` То же самое, но запрос одновременно привязывает вам аккаунт

После выполнения этой команды, в `/joins` всех бесед сервера появится новая строка, а во всех беседах, где включены уведомления о вступлении (`/join_notifications true`), появится сообщение, что пользователь отправил запрос на вступление в текущий сервер.

Модераторы любой из бесед сервера могут ответить на запрос:

- 🔨 `/accept <пользователи>` Принять приглашение. При этом пользователь добавляется во все `[welcome]`-каналы, то есть во все беседы, где выполнено `/newcomers_invited true`
- 🔨 `/deny <пользователи>` Отклонить приглашение. При этом пользователь может отправить повторный запрос, если посчитает, что его не приняли по ошибке
- 🔨 `/deny_ban <пользователи>` Отклонить приглашение и запретить пользователям посылать запросы на вступление в этот сервер. При этом пользователь вносится в особый список (не `banned`). Убрать пользователя из него можно через `/accept` или `/deny`

Другие команды:

- 👑 `/newcomers_invited <on/off>` Приглашать ли пользователей, принимаемых через `/accept`, в эту беседу сервера (по умолчанию `off`)
- 👑 `/join_notifications <on/off>` Показывать ли уведомления о новых запросах на вступление в этой беседе сервера

- 🔨 `/joins` Список неотвеченных запросов на вступление в беседу
- 🔨 `/joins_history` История запросов на вступление в беседу
